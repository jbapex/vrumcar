/**
 * Traço dos ícones Lucide no app (sidebar, topbar, etc.).
 * Padrão do Lucide é 2; ~1.35 fica um pouco mais grosso que o “fino” sem chegar no default.
 */
export const LUCIDE_STROKE_THIN = 1.35 as const;
